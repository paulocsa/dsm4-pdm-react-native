import { StyleSheet } from "react-native";

export default StyleSheet.create({
  input: {
    height: 50,
    borderColor: "#ddd",
    borderWidth: 1,
    borderRadius: 8,
    paddingHorizontal: 16,
    fontSize: 16,
    backgroundColor: "#fff",
    marginVertical: 12,
  },
});
